package sanctuary;

/**
 * CurrentLocStatus is an enumeration which
 * hold the 4 possible locations where a primate can be  :
 * ISOLATION, ENCLOSURE, TRANSFER, UNASSIGNED.
 */
public enum CurrentLocStatus {
  ISOLATION, ENCLOSURE, TRANSFER, UNASSIGNED
}
